#include <stdio.h>

int simple_calc()
{
	char arr[];

	printf("simple calculator with the following operations; '-', '+', '*', '/'\n");
	printf("to use type num space operation space number. (e.g 3 * 5) then enter\n");
	scanf("%s", &arr);
	for(i = 0; arr[i] != NULL; i++)
	{
		if(arr[i] !=<F11><F11>
